# Generated exports (one .txt per conversation window)
agent_logs/

# Runtime files
*.log
*.state.json
agentlog.pid

# Python
__pycache__/
*.pyc

# Local agent config (users keep their own)
agents.json

# Generated launcher shortcuts (machine-specific absolute paths)
# Ignore all .lnk files except the one in the repo root
*.lnk
!./agentlog-gui.lnk

# PyInstaller build artifacts
build/
dist/
*.spec

# Icon is committed, but a leftover build artifact if regenerated elsewhere
# (agentlog.ico is tracked on purpose)
